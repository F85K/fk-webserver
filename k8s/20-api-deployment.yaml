# FastAPI Deployment
apiVersion: apps/v1
kind: Deployment
metadata:
  name: fk-api
  namespace: fk-webstack
spec:
  replicas: 1
  selector:
    matchLabels:
      app: fk-api
  template:
    metadata:
      labels:
        app: fk-api
    spec:
      containers:
        - name: fk-api
          image: fk-api:latest
          imagePullPolicy: IfNotPresent
          ports:
            - containerPort: 8000
          env:
            # Mongo connectie settings
            - name: MONGO_URL
              value: mongodb://fk-mongodb:27017
            - name: MONGO_DB
              value: fkdb
            - name: MONGO_COLLECTION
              value: profile
            - name: NAME_KEY
              value: name
            - name: DEFAULT_NAME
              value: Frank Koch
          livenessProbe:
            # Liveness probe: herstart bij probleem
            httpGet:
              path: /health
              port: 8000
            initialDelaySeconds: 15
            periodSeconds: 10
            timeoutSeconds: 5
            failureThreshold: 3
          readinessProbe:
            # Readiness probe: pas verkeer sturen als gezond
            httpGet:
              path: /health
              port: 8000
            initialDelaySeconds: 10
            periodSeconds: 5
            timeoutSeconds: 3
            failureThreshold: 2
          resources:
            # INCREASED: FastAPI needs more memory for MongoDB connections
            requests:
              cpu: "100m"
              memory: "256Mi"
            limits:
              cpu: "500m"
              memory: "512Mi"
