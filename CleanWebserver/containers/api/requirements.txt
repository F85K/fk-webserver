# FK Webstack - API Dependencies
# Installed inside Docker container via: pip install -r requirements.txt

# Web framework
fastapi==0.115.6           # REST API framework
uvicorn[standard]==0.30.6  # ASGI server for FastAPI

# Database
pymongo==4.8.0             # MongoDB Python driver

# Optional but recommended
python-dotenv==1.2.1       # For .env file support (local development)
